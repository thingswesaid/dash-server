type Post {
  id: ID! @unique
  published: Boolean! @default(value: "false")
  title: String!
  content: String!
}

type User {
  id: ID! @unique
  firstName: String
  lastName: String
  email: String! @unique
  phone: String
  ips: [String!]
  subscribed: Boolean! @default(value: "true")
  videos: [Video]!
  status: Status! @default(value: "ACTIVE")
  paymentIds: [String]! @default(value: [])
}

type Video {
  id: ID! @unique
  name: String!
  link: String!
  preview: String!
  image: String!
  users: [User]!
  published: Boolean! @default(value: "false")
  amount: Float
  start: Int!
  orderIds: [String]! @default(value: [])
}

type Product {
  id: ID! @unique
  link: String!
  title: String!
  description: String!
  image: String!
}

type AnonymousIp {
  id: ID! @unique
  status: Status! @default(value: "ACTIVE")
  ip: String!
}

enum Status {
  ACTIVE
  BLOCKED
}