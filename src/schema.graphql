type Query {
  feed: [Post!]!
  drafts: [Post!]!
  post(id: ID!): Post
  videos(id: ID!): [Video]
  userIp: String
}

type Mutation {
  createDraft(title: String!, content: String): Post
  deletePost(id: ID!): Post
  publish(id: ID!): Post
}

type User {
  id: ID!
  email: String!
  ips: [String!]
  subscribed: Boolean!
  videos: [Video]
}

type Video {
  id: ID! 
  link: String!
  preview: String!
  image: String!
  users: [User]
}

type Post {
  id: ID!
  published: Boolean!
  title: String!
  content: String!
}