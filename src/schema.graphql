type Query {
  feed: [Post!]!
  drafts: [Post!]!
  post(id: ID!): Post
  videos(id: ID!): [Video]
}

type Mutation {
  createDraft(title: String!, content: String): Post
  deletePost(id: ID!): Post
  publish(id: ID!): Post
}

type User {
  id: ID!
  email: String!
  ips: [String!]
  subscribed: Boolean!
}

type Video {
  id: ID! 
  link: String!
  preview: String!
  image: String!
  users: [User]
}

type Post {
  id: ID!
  published: Boolean!
  title: String!
  content: String!
}